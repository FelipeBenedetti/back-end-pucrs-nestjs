// src/domain/repositories/icliente-repository.js
//import { Cliente } from '../entities/clienteEntity'
const { Cliente } = require('../entities/clienteEntity');
export class IClienteRepository {
    findById(id) {
        throw new Error('Método não implementado');
    }
    findAll() {
        throw new Error('Método não implementado');
    }
    save(cliente) {
        throw new Error('Método não implementado');
    }
}

